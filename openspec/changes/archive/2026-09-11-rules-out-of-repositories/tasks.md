## 1. The domain vocabulary — additive

Nothing in this group edits an existing file, so it lands with the current suite still green.

- [x] 1.1 Write `apps/api/src/orders/domain/day-window.ts`: `IDayWindow { start: Date; end: Date }`, `dayWindow(day: Date): IDayWindow` ending at the **next local midnight** (`new Date(day.getFullYear(), day.getMonth(), day.getDate() + 1)`), and `isInWindow(instant: Date | undefined, window: IDayWindow): boolean` returning `false` for an undefined instant. Verify: `npm test -w apps/api` still green; `git status` shows no edited file.
- [x] 1.2 Write `day-window.spec.ts`: pins an instant inside and outside the window, the last millisecond of the day, both edges of a month, and a year boundary; asserts an undefined instant is not in the window. Verify: `npm test -w apps/api -- day-window` reports the new file collected and passing (never `-w` on the vitest command line — vitest reads it as `--watch`).
- [x] 1.3 Write `apps/api/src/orders/domain/order-progress.ts`: `IN_PROGRESS_STATUSES_BY_TYPE: Readonly<Record<EOrderType, readonly EOrderStatus[]>>` holding Local `[Open]` and Delivery `[Open, Preparing, Out for delivery]`, plus `isInProgress(order: Order): boolean` reading it. Verify: the `Record` makes a removed key a compile error — delete one key, watch `npm run build -w apps/api` fail, restore it (design decision 1's totality guarantee).
- [x] 1.4 Write `order-progress.spec.ts`: a Local order that is Open is in progress and one that is Closed is not; a Delivery order is in progress at each of its three statuses and not at Delivered or Cancelled. Verify: same command as 1.2.
- [x] 1.5 Write `apps/api/src/orders/domain/order-sales.ts`: `SALES_STATUSES_BY_TYPE` (Local → `Closed`, Delivery → `Delivered`), `salesInstantOf(order): Date | undefined` (`closedAt` for a closed local, `deliveredAt` for a delivered delivery, `undefined` otherwise), and `isSaleOn(order, window): boolean`. Verify: as 1.2.
- [x] 1.6 Write `order-sales.spec.ts`: a closed local counts by `closedAt` and not by `deliveredAt`; a delivered delivery counts by `deliveredAt` and not by `closedAt`; a cancelled order never counts; a `Closed` order carrying no `closedAt` never counts. Verify: as 1.2.

## 2. The use-cases take the rules

- [x] 2.1 Rewrite `ListOrdersUseCase` around a private `isListedOn(order, day)`: filter the entries the repository returns (day window ∪ `isInProgress`), then map as today. Its header comment currently claims a rule the body does not implement — check that the finished body makes it true, and correct the comment if not. Verify: `list-orders.spec.ts` gains the four scenarios from `openspec/specs/orders/listing/spec.md` over literal `Order`s and a doubled repository — an order left open overnight stays listed; a delivery still in its cycle from an earlier day stays listed; an earlier day's completed orders stay out; the day's own orders are listed whatever their status.
- [x] 2.2 Give `ListDaySalesUseCase` a private `isSaleOn` built from `order-sales.ts`, filtering what it receives. Verify: `list-day-sales.spec.ts` asserts the population over literal orders — incomplete, cancelled and earlier-day orders excluded, the day's closed locals and delivered deliveries included.
- [x] 2.3 Add `existsTable(tableId: string): Promise<boolean>` to `IOrdersRepository` and implement it in `PrismaOrdersRepository` with `this.prisma.table.count({ where: { id: tableId } }) > 0`. Verify: `npm run build -w apps/api` passes; the `implements` clause is what makes a missing implementation a compile error.
- [x] 2.4 Have `CreateOrderUseCase` refuse a local order whose `tableId` names no registered table, throwing `'Table not found'` — before `Order.create`, beside the existing `'Table already has an open order'` check. Verify: `create-order.spec.ts` asserts an unknown `tableId` is refused, which it does not today.
- [x] 2.5 Sort in `ListTablesUseCase`: order its own output by `number` ascending, so the rule is stated where it is testable. Verify: `tables-use-cases.spec.ts` asserts ascending order from a deliberately unsorted `findAll` double.

## 3. The adapters become translators

- [x] 3.1 In `PrismaOrdersRepository`, rebuild `openOrderConditions()` from `IN_PROGRESS_STATUSES_BY_TYPE` and `completedWhere()` from `SALES_STATUSES_BY_TYPE` + `salesInstantOf`'s column choice, so no status literal is written in the adapter. Verify: `grep -n "'Open'\|'Preparing'\|'Out for delivery'\|'Delivered'\|'Closed'" src/orders/infrastructure/prisma-orders-repository.ts` returns only the `tableId`-keyed line of 3.3, if any.
- [x] 3.2 Delete the adapter's private `dayWindow` and `MS_PER_DAY`; call `dayWindow` from `orders/domain/day-window.ts`. Verify: `grep -n "MS_PER_DAY" src/orders/` returns nothing.
- [x] 3.3 State `findOpenByTableId`'s rule as `{ type: EOrderType.LOCAL, status: EOrderStatus.OPEN, tableId }` — the order that occupies a table is a *local* open one, which the current `{ status: 'Open', tableId }` leaves implicit. Verify: `npm test -w apps/api` green. **Correction:** this task originally claimed the behavior was unchanged "because a delivery order carries no `tableId`". That is false — `CreateOrderDto` accepts `tableId` and `Order.create` does not forbid it on a `DELIVERY`, so such an order used to be returned by `findOpenByTableId` and no longer is. No test covered it then or now; the gap is recorded in `08-conventions.md` to be closed on the write side.
- [x] 3.4 Keep `save()`'s `P2003 → 'Table not found'` catch with a comment saying it is a backstop for a table deleted between 2.4's check and the insert, not the rule's home (design decision 3). Verify: `apps/api/.claude/rules/09-comments.md`'s comment discipline — the comment says *why the mirror exists*, which is its case 1.
- [x] 3.5 Drop `orderBy: { number: 'asc' }` from `PrismaTablesRepository.findAll`, now that 2.5 sorts. Verify: `grep -n "orderBy" src/tables/infrastructure/prisma-tables-repository.ts` returns nothing.
- [x] 3.6 Confirm the two clean adapters stay untouched: `PrismaCatalogRepository` and `PrismaUserRepository` carry no rules to move. Verify: `git diff --stat src/catalog/infrastructure/prisma-catalog-repository.ts src/users/infrastructure/prisma-user-repository.ts` is empty.

## 4. Reaching the goal without deletions

**Superseded by decision on 2026-09-11: nothing is deleted.** Design decisions 6 and 7 planned to reach "no Postgres in `npm test`" by removing the specs that needed one; the goal is instead reached by removing the *dependency*, so every spec that exists today still exists. 4.2, 4.3 and 4.4 assumed deletions that are not shipping; 4.1 and 4.5 were re-scoped to the same goal.

- [x] 4.1 **Rewrite** the four adapter specs onto a Prisma double — no deletion: `src/orders/infrastructure/prisma-orders-repository.spec.ts`, `src/catalog/infrastructure/prisma-catalog-repository.spec.ts`, `src/tables/infrastructure/prisma-tables-repository.spec.ts`, `src/users/infrastructure/prisma-user-repository.spec.ts`. Each constructs its repository with `new PrismaXRepository(double as unknown as PrismaService)` and asserts three things: the aggregate it builds from fake rows, the `where`/`include`/`orderBy` it asks Prisma for (expected values written out literally, so a hardcoded status literal or a dropped order type fails), and the `P2003 → 'Table not found'` translation via a constructed `PrismaClientKnownRequestError`. The `where` assertions are where design decision 2's accepted gap now gets its teeth: they do not prove Postgres honours the query, but they do prove the adapter mirrors the domain's rule tables. Verify: `npm test -w apps/api` green with `DATABASE_URL` pointed at an unreachable address.
- [x] 4.2 Superseded — the three technology specs are pure (`env.validation`, `cors`, `domain-error.filter` take no Prisma, no `Test.createTestingModule`) and already ran without a database. They stay.
- [x] 4.3 Superseded — the 8 e2e specs and `vitest.config.e2e.ts` pass today and stay.
- [x] 4.4 Superseded — `package.json` keeps `test:e2e`, `supertest` and the `test/` globs, since `test/` stays.
- [x] 4.5 Clean `vitest.config.ts`: drop `fileParallelism: false` and the comment about integration specs truncating a shared database. Now that no spec opens a connection, serialising files buys nothing — the suite went from 4.3s to 0.7s. Verify: `npm test -w apps/api` green.
- [x] 4.6 Prove the goal the proposal claims: the api suite needs no Postgres, no Docker and no `.env.local`. Verify: with `DATABASE_URL` and `TEST_DATABASE_URL` pointed at `127.0.0.1:1`, `npm test -w apps/api` passes and reports **36 spec files, 349 tests** — no spec file was removed, and the change's earlier count of 33 (and the 26 this task predicted) was already stale.

## 5. The rulebooks catch up

- [x] 5.1 `apps/api/.claude/rules/02-testing.md`: the pyramid table's Integration row described a layer that no longer exists (`npm test` opens no connection), and the Layout section said "7 today" while eight existed. Rewritten to two layers (unit + e2e), with the adapter specs named as unit tests that double `PrismaService`, the "7 today" list corrected to the eight real files, and "real persistence belongs in integration tests" retargeted at `test/`.
- [x] 5.2 `apps/api/.claude/rules/01-project-context.md`: the `npm test` row now reads "no database, no Docker, no `.env.local`". The `npm run test:e2e` row, the `lint`/`format` globs and the `docker compose` framing are all still accurate, since `test/` and e2e stay.
- [x] 5.3 `apps/api/.claude/rules/07-prisma.md` rule 3: "except the integration tests, which build the real client against the test database" was false and is replaced by the e2e suite as the only thing that builds a real client, with the repository specs named as doubling `PrismaService` instead. (`grep -n integration .claude/rules/07-prisma.md` now returns nothing — rule 5 never mentioned them.)
- [x] 5.4 `apps/api/.claude/rules/08-conventions.md`: updated the `EOrderStatus` item, which claimed the adapter "compensates" — the rule tables in the domain do now. Added two entries: the **behaviour change** 3.3 shipped (a `Delivery` carrying a `tableId` no longer occupies the table, reachable through `CreateOrderDto`, untested) and **what `npm test` structurally cannot prove** (that Postgres honours the `where`, and that a domain `Error` reaches the client as `400` — `test/`'s e2e specs are the closure; the filter's own spec pins only the mapping).
- [x] 5.5 Root `CLAUDE.md`: "`apps/api/.env.local` is required for the api suite to run at all" is now false — it is required by `npm run dev:api` and `npm run test:e2e` only.

## 6. The closing gate

- [x] 6.1 Run, in order: `npm run lint`, `npm run build`, `npm test -w apps/api` with no `DATABASE_URL`/`TEST_DATABASE_URL` in the environment, `npm test -w apps/web`. Verify: all green — api 36 files / 349 tests in 0.7s, web 76 files / 479 tests, lint and build clean. (Unsetting the two variables replaces the planned rename of `.env.local`: the only specs that read that file were the adapter specs, and they no longer do.)
- [x] 6.2 Format only the files this change touched, with a scoped `npx prettier --write <files>` — never the api's `npm run format`, whose glob reaches `src/prisma/generated/` and rewrites the committed client. Verify: `git status --porcelain apps/api/src/prisma/generated/` is empty.
- [x] 6.3 `openspec validate rules-out-of-repositories --strict`. Verify: passes, with `skip_specs: true` in `.openspec.yaml` covering the zero-delta change.
